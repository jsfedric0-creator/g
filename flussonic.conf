# Basic configuration
http 0.0.0.0:80;
rtmp 0.0.0.0:1935;
rtsp 0.0.0.0:554;

# Disable features to save memory
# edit_auth off;
# transcoder off;

# Simple test stream
stream test {
    input fake://fake;
    transcoder vb=800k;
}

# Health check endpoint
api;
